import { FC } from 'react';

interface DownSubNavbarProps {

};

const DownSubNavbar: FC<DownSubNavbarProps> = ({ }) => {
    return (
        <div>
            DownSubNavbar
        </div>
    );
};

export default DownSubNavbar;