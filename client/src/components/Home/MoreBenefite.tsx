import { FC } from 'react';

interface MoreBenefiteProps {

};

const MoreBenefite: FC<MoreBenefiteProps> = ({ }) => {
    return (
        <div>
            MoreBenefite
        </div>
    );
};

export default MoreBenefite;