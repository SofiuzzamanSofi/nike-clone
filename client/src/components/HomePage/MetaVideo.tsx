import { FC } from 'react';

interface MetaVideoProps {

};

const MetaVideo: FC<MetaVideoProps> = ({ }) => {
    return (
        <div>
            MetaVideo
        </div>
    );
};

export default MetaVideo;